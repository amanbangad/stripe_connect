# Platform's Stripe test-mode secret key
STRIPE_SECRET_KEY=sk_test_...

# Signing secret from `stripe listen --print-secret` (Stripe CLI)
STRIPE_WEBHOOK_SECRET=whsec_...

PORT=4242

# Deferred-onboarding risk caps (see lib/store.js)
MAX_HELD_ORDERS_PER_COURIER=3
MAX_HELD_CENTS_PER_COURIER=5000
